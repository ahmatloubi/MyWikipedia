import XCTest
@testable import MyWikipediaViewModel

final class MyWikipediaViewModelTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyWikipediaViewModel().text, "Hello, World!")
    }
}
