import XCTest
@testable import MyWikipediaModel

final class MyWikipediaModelTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyWikipediaModel().text, "Hello, World!")
    }
}
